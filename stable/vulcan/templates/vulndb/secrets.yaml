{{- $c := (dict "comp" .Values.vulndb "context" $) -}}
{{- if and $c.comp.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "vulndb.name" . }}
  labels: {{- include "vulcan.labels" . | nindent 4 }}
    app.kubernetes.io/component: vulndb
type: Opaque
data:
  PG_PASSWORD: {{ $c.comp.db.password | b64enc | quote }}
{{- end }}
